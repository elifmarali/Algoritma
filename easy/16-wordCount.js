export const wordCount = (str) => {
    let strArrLength = str.trim().split(" ").length;
    return strArrLength
}
/* Word Count
Using the JavaScript language, have the function WordCount(str)
take the str string parameter being passed and return
the number of words the string contains
(le. "Never eat shredded wheat" would return 4) -
Words will be separated by single spaces. */